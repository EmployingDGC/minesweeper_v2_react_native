import React from "react";
import { View } from "react-native";

import styles from "./styles";

const Board = (props) => {
    return (
        <View>

        </View>
    );
}

export default Board;
